package org.jwellman.jpreechart;

import java.util.Arrays;

/**
 *
 * @author rwellman
 */
public class DeleteMe {

    public void test() {
        String[] rawarray = {"z", "a", "m"};
        Arrays.sort(rawarray);
        Arrays.copyOf(rawarray, rawarray.length);
    }
}
