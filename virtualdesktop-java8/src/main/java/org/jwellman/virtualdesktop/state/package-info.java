/**
 * Redux-style application state management.
 *
 * <p>The subpackages define actions, immutable state models, reducers, and the
 * central store used to coordinate state changes across desktop features.
 * Swing-facing subscribers are notified on the event dispatch thread.</p>
 */
package org.jwellman.virtualdesktop.state;
